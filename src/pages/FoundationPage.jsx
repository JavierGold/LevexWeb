export default function FoundationPage({ title }) {
  return (
    <section className="foundation-page">
      <title>{`${title} | LEVEX`}</title>
      <h1 className="foundation-page__title">{title}</h1>
    </section>
  )
}
